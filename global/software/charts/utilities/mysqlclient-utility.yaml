---
schema: armada/Chart/v1
metadata:
  schema: metadata/Document/v1
  name: mysqlclient-utility
  layeringDefinition:
    abstract: false
    layer: global
  labels:
    name: mysqlclient-utility-global
    component: utility
  storagePolicy: cleartext
  substitutions:
    # Chart source
    - src:
        schema: pegleg/SoftwareVersions/v1
        name: software-versions
        path: .charts.utility.mysqlclient-utility
      dest:
        path: .source

    # Images
    - src:
        schema: pegleg/SoftwareVersions/v1
        name: software-versions
        path: .images.utility.mysqlclient-utility
      dest:
        path: .values.images.tags

    # Log User mysqlclient Utility Container
    - src:
        schema: nc/CorridorConfig/v1
        name: corridor-config
        path: .utility.always_log_user
      dest:
        path: .values.conf.utility.always_log_user

data:
  chart_name: mysqlclient-utility
  release: mysqlclient-utility
  test:
    enabled: false
  namespace: utility
  protected:
    continue_processing: false
  wait:
    timeout: 600
    labels:
      release_group: clcp-mysqlclient-utility
  install:
   no_hooks: false
  upgrade:
   no_hooks: false
   pre:
      delete:
        - type: job
          labels:
            release_group: clcp-mysqlclient-utility
  values:
    labels:
      utility:
        node_selector_key: utility
        node_selector_value: enabled
  dependencies:
    - mysqlclient-utility-htk
...
---
schema: armada/Chart/v1
metadata:
  schema: metadata/Document/v1
  name: mysqlclient-utility-htk
  layeringDefinition:
    abstract: false
    layer: global
  storagePolicy: cleartext
  substitutions:
    - src:
        schema: pegleg/SoftwareVersions/v1
        name: software-versions
        path: .charts.utility.mysqlclient-utility-htk
      dest:
        path: .source
data:
  chart_name: mysqlclient-utility-htk
  release: mysqlclient-utility-htk
  namespace: mysqlclient-utility-htk
  values: {}
  dependencies: []
...
