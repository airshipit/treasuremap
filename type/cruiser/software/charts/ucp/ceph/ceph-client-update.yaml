---
schema: armada/Chart/v1
metadata:
  schema: metadata/Document/v1
  name: ucp-ceph-client-update
  labels:
    name: ucp-ceph-client-update-nc
  layeringDefinition:
    abstract: false
    layer: type
    parentSelector:
      name: ucp-ceph-client-update-global
    actions:
      - method: replace
        path: .values.conf.pool.target.osd
      - method: merge
        path: .
  storagePolicy: cleartext
data:
  values:
    conf:
      pool:
        target:
          osd: 24
...

