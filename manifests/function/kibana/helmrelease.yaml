apiVersion: "helm.toolkit.fluxcd.io/v2beta1"
kind: HelmRelease
metadata:
  name: kibana
spec:
  releaseName: kibana
  interval: 5m
  chart:
    spec:
      sourceRef:
        kind: HelmRepository
        name: kibana
      interval: 1m
  timeout: 5m
  values: {}
