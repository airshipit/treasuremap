resources:
- namespace.yaml
- crds.yaml
- common.yaml
- operator.yaml
patchesStrategicMerge:
- |-
  kind: ConfigMap
  apiVersion: v1
  metadata:
    name: rook-ceph-operator-config
    namespace: rook-ceph
  data:
    CSI_ENABLE_HOST_NETWORK: "true"
